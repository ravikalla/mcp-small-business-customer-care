# Default configuration - STDIO mode for Claude Desktop compatibility
spring.profiles.active=stdio

# Common configuration
spring.application.name=small-business-customer-care
spring.ai.mcp.server.name=small-business-customer-care
spring.ai.mcp.server.version=1.0.0

# Default to STDIO mode (will be overridden by profiles)
spring.main.web-application-type=none
spring.main.banner-mode=off
logging.pattern.console=